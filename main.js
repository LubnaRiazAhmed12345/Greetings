// Greetings: Start with the array you used in Exercise 11, 
//but instead of just printing each person’s name, print a message to them. The text of each message should be the same, 
//but each message should be personalized with the person’s name.
var names = ["Lubna", "Kanwal", "Aqsa", "Huma"];
console.log("Birthday Invetation,".concat(names[0]));
console.log("Birthday Invetation,".concat(names[1]));
console.log("Birthday Invetation,".concat(names[2]));
console.log("Birthday Invetation,".concat(names[3]));
